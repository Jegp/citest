apply plugin: 'java'

repositories {
 mavenCentral()
}

dependencies {
  testCompile 'junit:junit:4.12'
}

task gitTag(type: Exec) {
 commandLine 'sh', '-c', 'git tag -a $(($(git describe --tag) + 1)) -m "New release"'
}